; load [global] util/language

; validate_instruction
Check following informations then click on "validate" to end installation.

; fld-general-information
General informations

; info_language
Language :

; fld-server-information
Server informations

; info_servername
Name :

; error_mainconfig_hosts(save)
Possible errors :
1. Invalid server name
2. Invalid domain name
3. Impossible configuration server
4. A problem occured when generating configuration files

; info_serverdomain
Server domain :

; info_adminpasswd
Root password :

; error_mainconfig_adminpasswd(save)
Possible errors :
1. Invalid password
2. Databases cannot be joined

; info_netiface_address
IP address :

; info_netiface_netmask
Netmask :

; info_netiface_gateway
Default gateway :

; error_netiface(save)
Possible errors :
1. Invalid network configuration
2. Impossible configuration server
3. A problem occured when generating configuration files

; info_resolvconf_nameserver1
Primary DNS server :

; info_resolvconf_nameserver2
Secondary DNS server :

; error_resolvconf_nameservers(save)
Possible errors :
1. Invalid DNS configuration
2. Impossible configuration server
3. A problem occured when generating configuration files

; fld-entity
Entity

; info_entity_displayname
Printed name :

; error_entity(save)
Possible errors :
1. Impossible configuration server
2. A problem occured when generating configuration files

; fld-context
Contexts

; fld-defaultconfig
Default configuration

; fm_defaultconfig
Apply default configuration for France :

; info_context_internal
Internal calls :

; error_context_internal(save)
Possible errors :
1. Impossible configuration server
2. A problem occured when generating configuration files

; info_context_incall
Incalls :

; error_context_incall(save)
Possible errors :
1. Impossible configuration server
2. A problem occured when generating configuration files

; info_context_outcall
Outcalls :

; error_context_outcall(save)
Possible errors :
1. Impossible configuration server
2. A problem occured when generating configuration files

; entity_displayname
Display name : 

; internal_calls
Internal calls : 

; incoming_calls
Incoming calls : 

; outgoing_calls
Outgoing calls : 

; answer_false
No

; answer_true
Yes