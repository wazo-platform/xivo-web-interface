location /admin {
    return 302 $scheme://$host/index.php;
}

location /index.php {
    include /etc/nginx/xivo_params;
}

location ~ ^/(?:xivo|service|callcenter|cti|graphs|statistics)/ {
    index index.php;
    include /etc/nginx/xivo_params;
}

location /backup/ {
    rewrite ^/backup/(.*) /backup.php?path=$1 last;
    index index.php;
    include /etc/nginx/xivo_params;
}

location /dlbackup/ {
    internal;
    alias /var/backups/xivo/;
}

location ~ ^/monit {
    rewrite ^/monit/(.*) /$1 break;
    proxy_pass http://127.0.0.1:2812;
    allow 127.0.0.1;
    deny all;
}

location /server-status {
    stub_status on;
    access_log off;
    allow 127.0.0.1;
    deny all;
}

location /service/ipbx/web_services.php/phonebook/menu {
    proxy_pass https://$server_addr:9499/0.1/directories/menu/autodetect;
}

location /service/ipbx/web_services.php/phonebook/search {
    if ($arg_name) {
        proxy_pass https://$server_addr:9499/0.1/directories/lookup/autodetect?term=$arg_name;
    }

    proxy_pass https://$server_addr:9499/0.1/directories/input/autodetect;
}

## for debug purpose
#location /pgadmin {
#    alias /usr/share/phppgadmin;
#    index index.php;
#}

#location ~ ^/pgadmin/(.*\.php?)$ {
#    alias /usr/share/phppgadmin/$1;
#    include /etc/nginx/xivo_params;
#}
#location ~ ^/pgadmin/((?:themes|images|xloadtree)/.*)$ {
#    alias /usr/share/phppgadmin/$1;
#}

location ~* ^/(?:xivo|service|callcenter|cti|graphs|statistics|css|js|img|extra-libs)/.*\.(?:ico|css|js|gif|jpe?g|png|htm?l)$ {
    expires max;
    add_header Pragma public;
    add_header Cache-Control "public, must-revalidate, proxy-revalidate";
}


location /ws {
    proxy_pass http://localhost:5039;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_buffering off;
    proxy_connect_timeout 60m;
    proxy_read_timeout 60m;
    proxy_send_timeout 60m;
    keepalive_timeout 180s;
}

location / {
    proxy_pass  http://localhost:8070/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
}

location /xuc/ {
    proxy_pass http://localhost:8090/xuc/;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_buffering off;
    proxy_connect_timeout 60m;
    proxy_read_timeout 60m;
    proxy_send_timeout 60m;
}

location /ccagent {
    deny all;
}

location /ccmanager {
    deny all;
}
